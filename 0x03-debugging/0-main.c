#include "main.h"

/**
 * main- test function that indicates if an integer is
 * positive or negative
 *
 * Return: Always zero (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
